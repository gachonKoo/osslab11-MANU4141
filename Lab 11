import math

# calculate the length of hypotenuse(c) when a=3 and b=4
a, b = 3, 4
c = math.sqrt(a**2 + b**2)  # 피타고라스 정리
print('c =', c)

# calculate the area of circle with radius r = 10
r = 10
area = math.pi * r**2  # 원 넓이 공식
print('area =', area)
